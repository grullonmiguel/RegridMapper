<UserControl x:Class="RegridMapper.Views.RegridVerifyDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             Loaded="UserControl_Loaded">
    <Border
        Padding="24"
        Width="640"
        Height="480"
        VerticalAlignment="Center" 
        HorizontalAlignment="Center" 
        Style="{StaticResource Borders.Secondary}">

        <Border.RenderTransform>
            <TranslateTransform x:Name="SlideTransform" Y="-100"/>
        </Border.RenderTransform>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Viewbox Grid.Row="1" Width="32" Height="32">
                <Path Data="{StaticResource Geometry.Regrid}" />
            </Viewbox>

            <TextBlock 
                Grid.Row="2"
                Margin="0 24"
                TextWrapping="Wrap"
                Style="{DynamicResource TextBlocks.Heading1}" 
                Text="Ensure you are logged into Regrid and press CONFIRM to proceed." />
            
            <!-- Yes Button -->
            <Button 
                Grid.Row="3"
                Content="CONFIRM"
                HorizontalAlignment="Center"
                FontSize="{StaticResource Fonts.Small}"
                Style="{StaticResource Buttons.Primary}"
                Command="{Binding YesCommand}" />
        </Grid>
    </Border>
</UserControl>
